<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chapter8.aplikasinote">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiNote"
        tools:targetApi="31">
        <activity
            android:name=".view.activty.DetailNoteActivity"
            android:exported="false" />
        <activity
            android:name=".view.activty.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".view.activty.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activty.TambahNoteActivity"
            android:exported="false" />
        <activity
            android:name=".view.activty.MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>